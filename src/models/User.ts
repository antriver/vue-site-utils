export interface User {
    id: number;
    username?: string;
}

export interface UserGlobalOptions {
    bgColor?: string,
    bgColorTheme?: string;
    bgPattern?: string;
    darkMode?: boolean;
}
